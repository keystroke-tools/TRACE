{
  "name": "trace-workspace",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "pnpm --filter @trace/desktop --filter @trace/web build",
    "check": "pnpm --filter @trace/desktop --filter @trace/web check"
  }
}
